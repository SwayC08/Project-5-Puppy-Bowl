const Next = () => {

      // const [currentPlayer, setCurrentPlayer] = useState(players);
    // console.log(nextPlayer);
    // function cycle(){
        // for (let i=0; i < currentPlayer.length; i++){
        //         let nextPlayer = currentPlayer[i];
        //         console.log(nextPlayer);
                // setCurrentPlayer(nextPlayer);
            // }
        // console.log(setCurrentPlayer());
    // console.log(cycle());
    
    return (
        <div></div>
    )
}
export default Next;